(forward (int (int)) g)
(public f (function (int (int i))
  (int n 9)
  (if (== i 0)
      (set n 0)
      (set n (+ i (g (- i 1)))))
  (return n)
))
(public g (function (int (int i))
  (int n 9)
  (if (== i 0)
      (set n 0)
      (set n (+ i (f (- i 1)))))
  (return n)
))
(program
  (println (f 3))
  (return 0)
)

